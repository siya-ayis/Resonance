# Core analysis stack (verified on Python 3.12).
numpy>=1.26
scipy>=1.11
librosa>=0.10
soundfile>=0.12

# OPTIONAL — real source separation. Heavy (PyTorch + model download).
# Install separately to enable real stems; otherwise the pipeline uses
# HPSS + band-filtered pseudo-stems automatically.
#   pip install demucs torch
# demucs>=4.0
# torch>=2.0
